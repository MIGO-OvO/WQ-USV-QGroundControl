/****************************************************************************
 *
 * USV FlyView Bottom Right Row Layout - 无人船飞行视图右下角布局
 *
 * 完全隐藏原版的遥测数据条和仪表盘
 * USV 使用 FlyViewCustomLayer 中的自定义仪表盘
 *
 ****************************************************************************/

import QtQuick
import QtQuick.Layouts

// 空布局 - 隐藏原版的遥测数据条和仪表盘
RowLayout {
    // 不显示任何内容
    // USV 的仪表盘在 FlyViewCustomLayer 中实现
}
